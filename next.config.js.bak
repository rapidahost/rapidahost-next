import path from 'path'

/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    turbo: {
      loaders: {
        '.ts': '@vercel/turbo-loader',
      },
    },
  },
  compiler: {
    styledComponents: true,
  },
  webpack(config) {
    config.resolve.alias['@'] = path.resolve('./src')
    return config
  }
}

export default nextConfig
